#include <stdio.h>
int main()
{
	long long i;
	int j,temp,count=0;
	for(i=10000;i<99999;i++)
	{
		temp=i;
		while(temp)
		{
		j=temp%10;
		if(j==4)
		{	
			count++;
			break;
		}
		temp=temp/10;
		}
	}
	printf("%d",count);
}
